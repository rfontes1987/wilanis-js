{
  "$schema": "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/feature.schema.json",
  "label": "Access, for development",
  "description": "What lets the access tree run on its own: identity.port.json bound to the directories written in connections/. A host does not include this feature; it binds the port to its own directories.",
  "dependsOn": [
    "access"
  ],
  "exports": [],
  "effects": [
    "@auth/identity.port.json#verify",
    "@auth/token.port.json#issue",
    "@auth/token.port.json#refresh"
  ]
}

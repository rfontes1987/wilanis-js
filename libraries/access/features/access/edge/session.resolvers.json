{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/resolvers.schema.json",
  "label": "Session context",
  "description": "The session the guard identified, read by the data graphs that store and fetch its attributes.",
  "resolvers": {
    "sid": {
      "label": "The caller's session id",
      "read": "request.session.id",
      "description": "present on every trigger a signed-in policy gates: the guard verified the token and found its session, and the policy proves it (A006)",
      "required": true
    }
  }
}

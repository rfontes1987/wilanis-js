{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://wilanis.dev/schemas/1/feature.schema.json",
  "title": "feature",
  "description": "features/<name>/feature.json. The folder is the feature; every document under it belongs to it. This file says what the folder cannot: which features it may reference, what it exports to them, and which effectful native operations its data layer may reach.",
  "type": "object",
  "properties": {
    "$schema": { "const": "@wilanis/feature.schema.json" },
    "description": { "type": "string", "minLength": 1 },
    "dependsOn": { "type": "array", "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" }, "uniqueItems": true },
    "exports": { "description": "Paths other features may name: ports and core shapes.", "type": "array", "items": { "$ref": "common.schema.json#/$defs/path" }, "uniqueItems": true },
    "effects": { "type": "array", "items": { "$ref": "common.schema.json#/$defs/opRef" }, "uniqueItems": true }
  },
  "required": ["$schema", "description"],
  "additionalProperties": false
}

{
  "name": "wilanis-example-monitor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A wilanis project: pure JSON documents, run by @wilanis/runtime with the @http and @blob plugin packages, against a public REST API.",
  "scripts": {
    "check": "wilanis check .",
    "rehearse": "wilanis rehearse .",
    "fuzz": "wilanis fuzz .",
    "regress": "wilanis regress .",
    "digest": "wilanis run @monitor/edge/digest.trigger.json .",
    "start": "wilanis start ."
  },
  "dependencies": {
    "@wilanis/plugin-http": "0.1.0",
    "@wilanis/runtime": "0.1.0",
    "@wilanis/plugin-blob": "0.1.0",
    "@wilanis/plugin-reload": "^0.1.0"
  }
}

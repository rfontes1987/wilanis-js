{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/schemas-v1/packages/core/schemas/shape.schema.json",
  "label": "List request",
  "layer": "edge",
  "description": "GET /monitor?method=... as a client sends it.",
  "fields": {
    "method": {
      "type": "string",
      "enum": [
        "GET",
        "POST",
        "PUT",
        "PATCH",
        "DELETE"
      ],
      "required": false
    }
  }
}

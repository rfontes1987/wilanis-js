{ "$schema": "https://raw.githubusercontent.com/rfontes1987/wilanis/schemas-v1/packages/core/schemas/shape.schema.json", "layer": "core", "description": "A creation request as the domain receives it: no status yet, the domain decides.",
  "fields": { "title": { "type": "string" }, "assignee": { "type": "string", "required": false } } }

{ "$schema": "@wilanis/shape.schema.json", "layer": "core", "description": "How a listing may be narrowed.",
  "fields": { "status": { "type": "string", "enum": ["todo", "doing", "done"], "required": false } } }

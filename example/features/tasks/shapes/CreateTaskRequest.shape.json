{ "$schema": "@wilanis/shape.schema.json", "layer": "edge", "description": "POST /tasks body. Closed: an undeclared field is a 400, never a silent pass-through.",
  "fields": { "title": { "type": "string" }, "assignee": { "type": "string", "required": false } } }

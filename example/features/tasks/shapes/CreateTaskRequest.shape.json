{ "$schema": "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/shape.schema.json", "layer": "edge", "description": "POST /tasks body. Closed: an undeclared field is a 400, never a silent pass-through.",
  "fields": { "title": { "type": "string" }, "assignee": { "type": "string", "required": false } } }

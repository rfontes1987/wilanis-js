{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/trigger.schema.json",
  "title": "trigger",
  "description": "A way in. Names its kind (a document a plugin grants: an http route, a cli command, a topic...), the settings that kind's contract judges, the edge types it takes and answers, and the run node it fires: a domain port operation, with its inputs read from the kind's context. Nothing here knows what a URL is; the kind does.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "The kind of this document: its published schema URL, or the alias @wilanis/trigger.schema.json.",
      "enum": [
        "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/trigger.schema.json",
        "@wilanis/trigger.schema.json"
      ]
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "label": {
      "description": "A short human name for this document, shown wherever a reader sees it instead of its path. Absent: the file name stands in.",
      "type": "string",
      "minLength": 1
    },
    "kind": {
      "$ref": "common.schema.json#/$defs/path"
    },
    "settings": {
      "description": "The values the kind's settings contract judges. May read {{secrets.key}} and nothing else.",
      "type": "object"
    },
    "in": {
      "$ref": "common.schema.json#/$defs/typeRef"
    },
    "out": {
      "$ref": "common.schema.json#/$defs/typeRef"
    },
    "fire": {
      "description": "What this trigger invokes: one run node. `run` names a domain port operation (path#operation) and `in` gives every value it takes, reading {{request.*}} from the kind's context. The node type is not written -- a trigger fires exactly one operation, so this schema is the declaration. A trigger never names a graph: it states what it wants done, and the port's binding decides how.",
      "type": "object",
      "properties": {
        "description": {
          "type": "string"
        },
        "label": {
          "description": "A short human name for this call, shown wherever a reader sees it.",
          "type": "string",
          "minLength": 1
        },
        "run": {
          "$ref": "common.schema.json#/$defs/opRef"
        },
        "in": {
          "$ref": "common.schema.json#/$defs/values"
        }
      },
      "required": [
        "run"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "$schema",
    "description",
    "kind",
    "settings",
    "fire"
  ],
  "additionalProperties": false
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/binding.schema.json",
  "title": "binding",
  "description": "How a domain port is met. Each operation is bound to a data graph, or delegated to one native operation with its values fixed here -- the declared statement. A delegation's in may read {{in.*}} (the operation's accepts) and {{resolver.*}}; an input it does not give is passed from the operation's accepts by name.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "The kind of this document: its published schema URL, or the alias @wilanis/binding.schema.json.",
      "enum": [
        "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/binding.schema.json",
        "@wilanis/binding.schema.json"
      ]
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "port": {
      "$ref": "common.schema.json#/$defs/path"
    },
    "resolvers": {
      "$ref": "common.schema.json#/$defs/resolvers"
    },
    "operations": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "$ref": "common.schema.json#/$defs/ident"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "graph": {
            "$ref": "common.schema.json#/$defs/path"
          },
          "run": {
            "$ref": "common.schema.json#/$defs/opRef"
          },
          "in": {
            "$ref": "common.schema.json#/$defs/values"
          },
          "description": {
            "type": "string"
          }
        },
        "oneOf": [
          {
            "required": [
              "graph"
            ]
          },
          {
            "required": [
              "run"
            ]
          }
        ],
        "additionalProperties": false,
        "dependentSchemas": {
          "graph": {
            "description": "An operation bound to a graph is not delegated: no run, no in.",
            "properties": {
              "run": false,
              "in": false
            }
          }
        }
      }
    }
  },
  "required": [
    "$schema",
    "description",
    "port",
    "operations"
  ],
  "additionalProperties": false
}

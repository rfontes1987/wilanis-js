{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/connection.schema.json",
  "title": "connection",
  "description": "A named channel to an external system: its kind and the settings that kind judges. Values may read {{secrets.key}} and nothing else.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "The kind of this document: its published schema URL, or the alias @wilanis/connection.schema.json.",
      "enum": [
        "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/connection.schema.json",
        "@wilanis/connection.schema.json"
      ]
    },
    "description": {
      "description": "What this connection reaches, in one or two sentences.",
      "type": "string",
      "minLength": 1
    },
    "label": {
      "description": "A short human name for this document, shown wherever a reader sees it instead of its path. Absent: the file name stands in.",
      "type": "string",
      "minLength": 1
    },
    "kind": {
      "$ref": "common.schema.json#/$defs/path"
    },
    "settings": {
      "description": "The values the kind's settings contract judges. May read {{secrets.key}} and nothing else.",
      "type": "object"
    }
  },
  "required": [
    "$schema",
    "description",
    "kind",
    "settings"
  ],
  "additionalProperties": false
}

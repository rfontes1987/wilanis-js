{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/feature.schema.json",
  "title": "feature",
  "description": "features/<name>/feature.json. The folder is the feature; every document under it belongs to it. This file says what the folder cannot: which features it may reference, what it exports to them, and which effectful native operations its data layer may reach.",
  "type": "object",
  "properties": {
    "$schema": {
      "description": "The kind of this document: its published schema URL, or the alias @wilanis/feature.schema.json.",
      "enum": [
        "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/feature.schema.json",
        "@wilanis/feature.schema.json"
      ]
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "label": {
      "description": "A short human name for this document, shown wherever a reader sees it instead of its path. Absent: the file name stands in.",
      "type": "string",
      "minLength": 1
    },
    "dependsOn": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9-]*$",
        "description": "The name of a feature folder this one may reference: kebab-case."
      },
      "uniqueItems": true
    },
    "exports": {
      "description": "Paths other features may name: ports and core shapes.",
      "type": "array",
      "items": {
        "$ref": "common.schema.json#/$defs/path"
      },
      "uniqueItems": true
    },
    "effects": {
      "type": "array",
      "items": {
        "$ref": "common.schema.json#/$defs/opRef"
      },
      "uniqueItems": true
    }
  },
  "required": [
    "$schema",
    "description"
  ],
  "additionalProperties": false
}

{
  "$schema": "https://raw.githubusercontent.com/wilanis/wilanis-js/main/packages/core/schemas/plugin.schema.json",
  "label": "Storage in memory",
  "description": "An @storage engine that keeps records in a Map for as long as the process lives. It grants one connection kind and no port: what may be asked of a store is @storage's business, and this plugin only answers it. Nothing is written to disk and nothing survives a restart, which is what makes it right for development, for tests and for the trees under libraries/ -- and wrong for anything that must still be there tomorrow.",
  "grants": {
    "connectionKinds": [
      "@storage-memory/memory.connection-kind.json"
    ]
  }
}

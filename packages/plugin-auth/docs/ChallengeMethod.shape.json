{
  "$schema": "https://raw.githubusercontent.com/rfontes1987/wilanis-js/schemas-v1/packages/core/schemas/shape.schema.json",
  "label": "Challenge method",
  "description": "How a caller obtains a code for a challenge of this method. A policy outcome's `method` names one.",
  "layer": "edge",
  "fields": {
    "description": {
      "type": "string",
      "required": false
    },
    "obtain": {
      "type": "string",
      "description": "what the caller is told to do to get a code, {id} standing for the challenge id: a command to run, a message to expect"
    }
  }
}
